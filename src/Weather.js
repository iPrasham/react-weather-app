import React from 'react';
import "./Weather.css";

const Weather = function(props){
    return <div className="container">
        {/* <h5>{props.city}, {props.country}, {props.date}</h5> */}
    </div>
};

export default Weather;

